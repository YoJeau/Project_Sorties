<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid ms-5 me-5">
        <img src="{{ asset('img/logo.png') }}" alt="Logo Sortir.com" class="img-fluid rounded-circle" style="width: 50px; height: 50px; object-fit: cover;">
        <a class="navbar-brand ms-3" href="{{ path('app_home') }}">Sortir.com</a>
        <span class="small text-secondary d-none d-sm-block">sorties éducatives, souvenirs collaboratifs</span>

        <button class="navbar-toggler d-none d-lg-block" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end d-none d-lg-block" id="navbarColor01">
            <ul class="navbar-nav gap-5">
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_home') }}">Accueil</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_participant_my-profile') }}">Mon profil</a>
                    </li>
                    {% if app.user and is_granted('ROLE_ADMIN') %}
                        <li class="nav-item dropdown">
                            <a class="nav-link active dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Administration</a>
                            <div class="dropdown-menu">
                                <a class="dropdown-item" href="{{ path('app_participant_administration') }}">Participants</a>
                                <a class="dropdown-item disabled" href="#">Villes</a>
                                <a class="dropdown-item disabled" href="#">Sites</a>
                            </div>
                        </li>
                    {% endif %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_login') }}">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>

        <div class="d-block d-lg-none">
            <ul class="navbar-nav gap-5">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_login') }}">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

