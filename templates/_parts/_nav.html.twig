<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
    <div class="container-fluid ms-5 me-5">
        <a class="navbar-brand" href="#">Sortir.com</a>
        <img src="{{ asset('img/Sortir.com.png') }}" alt="Logo Sortir.com" class="img-fluid rounded-circle" style="width: 100px; height: 100px; object-fit: cover;">
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarColor01">
            <ul class="navbar-nav gap-5">
                {% if app.user and is_granted('ROLE_ADMIN') %}
                    <li class="nav-item">
                        <a class="nav-link active" href="#">Villes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="#">Sites</a>
                    </li>
                {% endif %}
                <li class="nav-item">
                    <a class="nav-link active" href="{{ path('app_home') }}">Accueil</a>
                </li>
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_participant_my-profile') }}">Mon profil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_logout') }}">Se déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link active" href="{{ path('app_login') }}">Se connecter</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>

