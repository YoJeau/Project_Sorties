{{ form_start(siteForm, { 'attr' : { 'class' : 'd-flex justify-content-center' } }) }}
<div class="d-flex flex-row gap-3 mt-3 w-25">
    {{ form_label(siteForm.sitName) }}
    {{ form_widget(siteForm.sitName) }}
    <button type="submit" class="btn btn-success">Ajouter</button>
</div>
{{ form_end(siteForm) }}